# Optional: powers the Smart Assistant with OpenAI (gpt-4o-mini)
# Without this, the assistant uses built-in trained responses (works great!)
# Add the same variable in Vercel → Settings → Environment Variables

OPENAI_API_KEY=sk-your-key-here
